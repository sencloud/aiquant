# Web icons

When you scaffold the platform with `flutter create -t app --platforms web .`
inside `flutter-app/`, Flutter will populate this folder with default PNG
icons:

* `Icon-192.png`
* `Icon-512.png`
* `Icon-maskable-192.png`
* `Icon-maskable-512.png`

You can then drop in your own brand icons of the same dimensions.
